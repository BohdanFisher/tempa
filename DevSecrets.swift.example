import Foundation

#if DEBUG
// SETUP: copy this file to  "Tempa - Visual Planner/Services/DevSecrets.swift"
// and paste your real Anthropic API key below. The real DevSecrets.swift is
// git-ignored, so the key never lands in version control.
enum DevConstants {
    static let anthropicAPIKey = "sk-ant-REPLACE-WITH-YOUR-KEY"
}
#endif
